name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    - name: Deploy to Nginx
      run: |
        sudo rsync -av --checksum --delete ./ /var/www/html/
        sudo nginx -t && sudo systemctl reload nginx
